# Backend origin for a split deployment (e.g. frontend on Vercel, backend on a
# container host). Leave unset for local dev and the docker/nginx setup, which
# both serve the API same-origin.
VITE_API_BASE=https://your-backend-host.example.com
