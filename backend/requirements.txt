# Milestone 4 — document ingestion
pypdf>=5.0
python-docx>=1.1
beautifulsoup4>=4.12
lxml>=5.0
python-pptx>=1.0
charset-normalizer>=3.3

# Milestone 5 — OCR (also requires the Tesseract binary at runtime)
pillow>=10.0
pytesseract>=0.3.10
pypdfium2>=4.30

# Milestone 8 — embeddings
httpx>=0.27
# optional local models: pip install sentence-transformers

# Milestone 9 — vector stores (Qdrant speaks REST via httpx above)
# optional pgvector backend: pip install psycopg[binary]

# dev / test
pytest>=8.0
ruff>=0.6
