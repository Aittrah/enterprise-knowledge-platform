# EKIP Backend

FastAPI application + Celery workers. Module layout is specified in [docs/02-architecture.md §2](../docs/02-architecture.md).

Implementation begins at Milestone 4 (Document Ingestion). Each milestone lands as its own feature branch with tests under `backend/tests/`.
