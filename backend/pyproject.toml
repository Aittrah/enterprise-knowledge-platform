[project]
name = "ekip-backend"
version = "0.1.0"
description = "Enterprise Knowledge Intelligence Platform — backend"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
